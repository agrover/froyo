language: rust

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libdbus-1

rust:
  - stable
  - beta
  - nightly
script:
  - cargo build --verbose
  - |
    [ $TRAVIS_RUST_VERSION != nightly ] ||
    cargo build --verbose --features clippy
